import type React from "react";

interface BuilderCourseLayoutProps {
  children: React.ReactNode;
}

export default function BuilderCourseLayout({
  children,
}: BuilderCourseLayoutProps) {
  return children;
}
